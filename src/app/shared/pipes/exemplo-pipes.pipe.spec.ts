import { ExemploPipesPipe } from './exemplo-pipes.pipe';

describe('ExemploPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new ExemploPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
